import React from 'react';
import './Header.scss';

function Header() {
  return (<header className="App-header">
    <div id="header">
    <h1>ReSTy</h1>
    </div>
  </header>);
}

export default Header;
